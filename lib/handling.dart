import 'package:flutter/material.dart';

class Handling extends StatelessWidget {
  // const Handling({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Yes!'),
    );
  }
}
